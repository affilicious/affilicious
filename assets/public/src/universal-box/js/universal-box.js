jQuery(function($) {
    $(".aff-product-image-gallery").lightSlider({
        item: 1,
        speed: 1000, // ms
        pause: 8000, // ms
        auto: true,
        loop: true,
        pager: false,
    });
});
